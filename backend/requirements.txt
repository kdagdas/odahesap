# OdaHesap backend — runtime dependencies.
# Deliberately lean: free-tier hosts (Render/Koyeb) build faster and stay
# inside memory limits without pandas/numpy/boto3, none of which are used.
fastapi==0.110.1
uvicorn[standard]==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
python-dotenv>=1.0.1
bcrypt==4.1.3
httpx>=0.27.0
tzdata>=2024.2
# Firebase Cloud Messaging HTTP v1 needs an OAuth2 access token signed with
# the service-account key; this mints it. No Firebase Admin SDK — that pulls
# in far more than we need for "send one push".
#
# The [requests] extra is required, not optional: google.auth.transport.requests
# imports `requests`, which google-auth does NOT pull in on its own. Without it
# the token exchange raises ImportError and every push silently no-ops.
google-auth[requests]>=2.35.0
